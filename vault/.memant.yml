# Memant node-vault marker. This repo's vault/ latches into any hub via 'memant sync'.
name: ant-paperclip
namespace: ant-paperclip        # cross-vault links use [[ant-paperclip/<page>]]
shareable: true         # ONLY shareable project knowledge here — it travels with the repo (git)
